***What was modified or rejected:***
 Overall few of ai’s modifications were used and a lot more were rejected over the past 4 weeks. The only rejection that's worth an explanation is the rescheduling(), as it is the only one with real evidence behind it. Rescheduling is in the v.02 scope list so the argument exists for it however due to the case for many ai suggestions there are no functional requirements that cover it, its not in the UML and its just out of scope. 

***What the AI left out, which matters more.*** 
The questions from the lab only required the AI to generate the appointment on its own and not the whole code, therefore nothing really enforced a lot of the requirements and the associations. 

***How the approved design constrained the AI.*** 
By giving the AI a set of guidelines to essentially follow, it limits AI response to invent and assume things, for example, with the duration of the appointment not being a requirement but ai insisted on putting it in. It wasn't exactly a question of if the code was good but if the features were required.
